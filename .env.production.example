# ========================================
# 邮件服务配置指南 
# ========================================

# 1. 基础配置
EMAIL_USE_MOCK=false  # 设为false启用真实邮件发送

# 2. Gmail 配置示例 (推荐)
# 步骤:
# - 登录Gmail -> 设置 -> 安全性 -> 两步验证
# - 生成应用专用密码
# - 使用该密码作为SMTP_PASSWORD

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-gmail@gmail.com
SMTP_PASSWORD=your-16-digit-app-password
SMTP_USE_TLS=true
FROM_EMAIL=your-gmail@gmail.com
FROM_NAME=智能工作流管理平台

# 3. 网易邮箱配置示例
# SMTP_HOST=smtp.163.com
# SMTP_PORT=587  
# SMTP_USERNAME=your-email@163.com
# SMTP_PASSWORD=your-authorization-code
# SMTP_USE_TLS=true
# FROM_EMAIL=your-email@163.com
# FROM_NAME=智能工作流管理平台

# 4. QQ邮箱配置示例
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@qq.com
# SMTP_PASSWORD=your-authorization-code
# SMTP_USE_TLS=true
# FROM_EMAIL=your-email@qq.com
# FROM_NAME=智能工作流管理平台

# 5. 企业邮箱配置示例 (腾讯企业邮)
# SMTP_HOST=smtp.exmail.qq.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@company.com
# SMTP_PASSWORD=your-password
# SMTP_USE_TLS=true
# FROM_EMAIL=your-email@company.com
# FROM_NAME=智能工作流管理平台

# 前端URL配置
FRONTEND_BASE_URL=http://localhost:5173

# ========================================
# 配置步骤说明
# ========================================

# Gmail配置步骤:
# 1. 登录Gmail账户
# 2. 进入 Google账户设置 -> 安全性
# 3. 启用两步验证
# 4. 生成应用专用密码
# 5. 将16位应用密码填入SMTP_PASSWORD

# 网易/QQ邮箱配置步骤:
# 1. 登录邮箱账户
# 2. 进入设置 -> POP3/SMTP/IMAP
# 3. 开启SMTP服务
# 4. 生成授权码
# 5. 将授权码填入SMTP_PASSWORD

# ========================================
# 安全注意事项
# ========================================

# 1. 不要在代码中硬编码邮箱密码
# 2. 使用环境变量存储敏感信息
# 3. 定期更换授权码/应用密码
# 4. 限制应用权限范围
# 5. 监控邮件发送日志